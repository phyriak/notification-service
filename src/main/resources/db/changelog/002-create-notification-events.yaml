databaseChangeLog:
  - changeSet:
      id: 002-create-notifications
      author: phyriak

      changes:
        - createTable:
            tableName: notifications

            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: user_id
                  type: varchar(255)
                  constraints:
                    nullable: true

              - column:
                  name: email
                  type: varchar(255)
                  constraints:
                    nullable: false

              - column:
                  name: type
                  type: varchar(255)
                  constraints:
                    nullable: false

              - column:
                  name: subject
                  type: varchar(255)
                  constraints:
                    nullable: false

              - column:
                  name: first_name
                  type: varchar(255)
                  constraints:
                    nullable: true

              - column:
                  name: message
                  type: varchar(5000)
                  constraints:
                    nullable: true

              - column:
                  name: event_id
                  type: uuid
                  constraints:
                    nullable: true

              - column:
                  name: status
                  type: varchar(50)
                  constraints:
                    nullable: false

              - column:
                  name: retry_count
                  type: integer

              - column:
                  name: created_at
                  type: timestamp
                  constraints:
                    nullable: false

              - column:
                  name: updated_at
                  type: timestamp
                  constraints:
                    nullable: false

        - addUniqueConstraint:
            tableName: notifications
            columnNames: event_id
            constraintName: uq_event_id