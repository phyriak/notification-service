spring:
  application:
    name: notification-service

  mail:
    host: smtp.poczta.onet.pl
    port: 587
    protocol: smtp

  kafka:
    listener:
      ack-mode: record

management:
  endpoints:
    web:
      exposure:
        include: info,health,prometheus

notification:
   system-email: ${EMAIL_USER:piotr_hyriak@op.pl}
   retry-count: 3