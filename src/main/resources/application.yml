spring:
  application:
    name: notification-service

  mail:
    host: ${EMAIL_HOST}
    port: ${EMAIL_PORT}
    protocol: smtp

  kafka:
    listener:
      ack-mode: record

management:
  endpoints:
    web:
      exposure:
        include: info,health,prometheus

notification:
   system-email: ${EMAIL_USER:piotr_hyriak@op.pl}
   retry-count: 3